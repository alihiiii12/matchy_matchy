# ماتشي ماتشي — matchy matchy

## المسارات الرسمية
| المكوّن | المسار |
|---------|--------|
| تطبيق Flutter | `c:\Users\hp\StudioProjects\matchy_matchy` |
| باك اند Laravel | `c:\xampp\htdocs\matchy_matchy` |
| قاعدة البيانات | `matchy_matchy` (MySQL) |

## التشغيل
```bash
# الباك اند
cd c:\xampp\htdocs\matchy_matchy
php artisan serve --host=0.0.0.0 --port=8000

# التطبيق
cd c:\Users\hp\StudioProjects\matchy_matchy
flutter pub get
flutter run
```

## Android Studio
افتح **هذا المجلد** فقط (`matchy_matchy`) — لا تفتح مجلد `zadak` القديم.
